public class MultipleTable {
    public static String multiTable(int num) {
        String result = new String();
        for (int i=1; i<11;i++){
            result = result + (i +" * " + num);
        }
        return "";
    }
}
