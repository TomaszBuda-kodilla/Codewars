public class LargestProduct {
    public static int solution(String number) {
        int result = 0;
        char[] stringToChar = number.toCharArray();
        /*for (int i=0;i<number.length()-4;i++){
            for (int j=0;j<number.length()-4;j++)
            {
                if(Integer.parseInt(String.valueOf(stringToChar[i]))+Integer.parseInt(String.valueOf(stringToChar[i+1]))){

                }
                result =
            }
        }*/
        return result;
    }
}
